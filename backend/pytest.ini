[tool:pytest]
# Configuración de pytest para Pool Banorte API - Vercel
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*

# Marcadores personalizados para Vercel
markers =
    vercel: tests para deploy en Vercel
    auth: tests de autenticación JWT en Vercel
    crud: tests de operaciones CRUD en Vercel
    integration: tests de integración con Supabase

# Opciones por defecto
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Variables de entorno para tests en Vercel
env =
    TESTING=true
    ENVIRONMENT=vercel

# Configuración de warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore:.*urllib3.*:DeprecationWarning